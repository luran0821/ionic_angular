
/*
angular.module('myapp')
    .controller('shoplistCtrl', function ($scope,$stateParams, dataFactory) {
       // var data = dataFactory.query();
        var id = $stateParams.id;
        console.log(id)
       /!* angular.forEach(data.productList.shoplist, function (item) {

            if (item.id == id) {
                $scope.product = item;

                return false;   // 中断forEach循环 <=> break
            }
        });*!/

      $scope.godscb=function(){

      }
        $scope.gofbk=function(){}

    })
*/
